[workspace]
name = "cpython"
channels = ["https://prefix.dev/conda-forge"]
platforms = ["osx-arm64"]
preview = ["pixi-build"]

[package]
name = "python"
version = "3.15a0"

[package.build]
backend = { name = "pixi-build-rattler-build", version = "*" }
